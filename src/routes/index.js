const { userRoutes } = require("./user-routes");
const { marketRoutes } = require("./market-routes");
const { productRoutes } = require("./product-routes");


module.exports = { userRoutes, marketRoutes, productRoutes }